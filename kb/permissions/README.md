# KB: Permissions

## What belongs here

- File/directory ownership issues (`chown`, `chgrp`)
- Mode and ACL issues (`chmod`, `setfacl`, `getfacl`)
- NAS user → Linux user mapping
- Samba force user/group behavior
- Mountpoint permission inheritance

## Naming

`YYYY-MM-DD_short_snake_case_topic.md`

## What does NOT belong here

- Samba config syntax (use `smb/`)
- Authentication/JWT issues (use `services/`)
- OS user management unrelated to NAS (use `linux/`)
